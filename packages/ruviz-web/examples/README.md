# ruviz Web Examples

The files in this directory are the source of truth for the VitePress gallery and live examples.

Build the SDK and serve the docs site locally:

```sh
bun install
bun run --cwd packages/ruviz-web docs:dev
```
