# Applications

This directory contains runnable integration applications built from the
repository's published packages.

- `web-demo/` is the private Vite and Playwright workspace used to exercise the
  web SDK in browsers.

Use the root Bun scripts such as `bun run dev:web`, `bun run build:web-demo`,
and `bun run test:web` so package dependencies are built consistently.
