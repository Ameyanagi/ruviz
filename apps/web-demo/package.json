{
  "name": "ruviz-web-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:sdk": "bun run --cwd ../../packages/ruviz-web build",
    "dev": "bun run build:sdk && vite",
    "build": "bun run build:sdk && vite build",
    "preview": "vite preview",
    "preview:ci": "vite preview --host 127.0.0.1 --port 4173",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "ruviz": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "vite": "^7.1.7"
  }
}
