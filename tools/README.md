# Developer Tools

This directory contains repository-maintenance and evaluation programs that are
not published as libraries or applications.

- `benchmarks/` contains reproducible cross-runtime and feature-impact harnesses.
- `gallery/` contains runnable Rust sources for visual experiments and gallery
  support work.

Generated output from these tools belongs under `generated/`; committed
publication assets remain under `docs/assets/`.
